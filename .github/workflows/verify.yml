name: Verify

# Runs on every push. Three things are being asserted:
#   1. The library code itself is correct — node:test against tools/lib/*.mjs.
#   2. Every edition passes the editorial gate.
#   3. The committed HTML is exactly what the JSON builds to — no hand-edited pages,
#      no drift between the data and the site.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Unit tests
        run: node --test

      - name: Editorial gate
        run: node tools/validate.mjs --strict

      - name: Build
        run: node tools/build.mjs

      - name: Assert the site matches the data
        run: |
          set -euo pipefail
          if ! git diff --quiet; then
            echo "::error::Committed HTML does not match what the JSON builds to."
            echo "Run 'node tools/build.mjs' locally and commit the result."
            git diff --stat
            exit 1
          fi
          echo "Site is in sync with generated/."
