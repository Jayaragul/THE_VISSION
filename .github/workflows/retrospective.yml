name: Legacy AI retrospective (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo "Retired to limit AI usage. Daily headlines use Wire and Digest; weekly-briefing.yml owns the single weekly AI allowance."
